spring:
    application:
        name: flood-gateway
    profiles:
        active: dev,native
    cloud:

        consul:
          host: localhost
          port: 8500
          config:
            format: key_value
    zipkin:
      enabled: false


info:
    project:
        version: #project.version#